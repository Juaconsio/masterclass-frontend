---
import { Icon } from 'astro-icon/components';
import AboutImage from '/src/assets/team.webp';
import { Image } from 'astro:assets';
const benefits = [
  {
    title: 'Apoyo humano',
    desc: 'Tutores que hablan tu mismo idioma y entienden tus dudas — sin clases que te dejan más perdido.',
  },
  {
    title: 'Comunidad activa',
    desc: 'Cientos de alumnos compartiendo tips y material clave. Ya no estudias solo.',
  },
  {
    title: 'Enfoque práctico',
    desc: 'Directo al grano: lo que realmente entra en la prueba, sin guías de hace diez años.',
  },
  {
    title: 'Sin horarios',
    desc: 'Estudia a tu ritmo con acceso total. Si a medianoche se te ocurre la duda, la resuelves.',
  },
];

const statLabel = '+500';
const statSuffix = 'estudiantes acompañados';
---

<div class="from-primary to-primary/75 bg-gradient-to-br from-60% to-100%">
  <section id="about" class="min-h-0 py-8 sm:py-10 md:py-12 lg:py-14">
    <div class="container mx-auto px-4 sm:px-6">
      <div class="grid items-center gap-8 lg:grid-cols-[1fr_minmax(280px,40%)] lg:gap-12 xl:gap-16">
        <div class="flex flex-col justify-center">
          <div
            class="border-accent/50 bg-accent/15 text-accent mb-4 inline-flex w-fit rounded-md border px-3 py-1.5 text-xs font-semibold tracking-wide uppercase sm:text-sm"
          >
            De estudiantes, para estudiantes
          </div>
          <h2
            class="font-display text-primary-content mb-4 text-3xl font-bold text-balance sm:mb-6 sm:text-4xl md:text-5xl"
          >
            Salva <span class="font-gebuk">Ramos</span>,<br />
            <span class="font-display">tu comunidad de apoyo</span>
          </h2>
          <p
            class="text-primary-content/85 mb-8 max-w-xl text-base leading-relaxed text-pretty sm:text-lg"
          >
            No somos solo una plataforma de clases. Somos una comunidad hecha para que no vuelvas a
            estudiar solo. Conectamos tu necesidad con la experiencia de quienes ya pasaron por lo
            mismo y salvaron el ramo.
          </p>
          <div class="grid gap-6 sm:grid-cols-2 sm:gap-x-8 sm:gap-y-8">
            {
              benefits.map((item) => (
                <div class="flex gap-3 sm:gap-4">
                  <div class="text-primary-content flex shrink-0 items-start pt-0.5">
                    <Icon
                      name="mdi:check-circle"
                      size={24}
                      class="sm:h-7 sm:w-7"
                      aria-hidden="true"
                    />
                  </div>
                  <div>
                    <h3 class="text-primary-content mb-1 font-semibold sm:text-lg">{item.title}</h3>
                    <p class="text-primary-content/80 text-sm leading-relaxed sm:text-base">
                      {item.desc}
                    </p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <div class="relative flex justify-center lg:justify-end">
          <div
            class="border-neutral-content/10 relative w-full max-w-md overflow-hidden rounded-2xl border shadow-xl sm:rounded-3xl lg:max-w-none"
          >
            <Image
              src={AboutImage}
              alt="Comunidad SalvaRamos: estudiantes y tutores en un entorno de apoyo"
              class="w-full object-contain"
              width="1024"
              height="1024"
              fetchpriority="low"
            />
            <div
              class="bg-primary text-primary-content absolute right-4 bottom-4 rounded-xl px-4 py-3 shadow-lg sm:right-6 sm:bottom-6 sm:px-5 sm:py-4"
              aria-hidden="true"
            >
              <span class="block text-2xl font-bold sm:text-3xl">{statLabel}</span>
              <span class="text-sm font-medium tracking-wide uppercase opacity-95 sm:text-base">
                {statSuffix}
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>
