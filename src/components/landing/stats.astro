---
import { Icon } from 'astro-icon/components';
---

<div>
  <!-- Stats Section -->
  <section
    class="flex flex-col justify-around items-center px-4 sm:px-6 py-8 sm:py-12 gap-6 sm:gap-8 lg:pt-4 lg:pb-8 bg-secondary rounded-2xl"
  >
    <h2
      class="py-2 sm:py-4 text-2xl sm:text-3xl md:text-4xl font-bold text-secondary-content text-center"
    >
      Hemos logrado grandes resultados
    </h2>
    <div
      class="flex flex-col gap-8 sm:gap-10 lg:gap-12 lg:flex-row justify-around w-full max-w-6xl text-secondary-content"
    >
      <div class="flex items-center gap-3 sm:gap-4 justify-center" data-usal="fade-l">
        <div class="flex items-center h-full gap-3 sm:gap-4 py-2 sm:py-4">
          <Icon name="mdi:book-open-variant" size={48} class="text-primary sm:w-16 sm:h-16" />
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold">+<span data-usal="count-[6]">6</span></div>
          <div class="w-28 sm:w-32 text-sm sm:text-base">años de experiencia</div>
        </div>
      </div>

      <div class="flex items-center gap-3 sm:gap-4 justify-center" data-usal="fade-u delay-[200]">
        <div class="flex items-center h-full gap-3">
          <Icon name="mdi:user-group" size={48} class="text-primary sm:w-16 sm:h-16" />
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold">
            + <span data-usal="count-[500]">500</span>
          </div>
          <div class="w-28 sm:w-32 text-sm sm:text-base">Estudiantes acompañados</div>
        </div>
      </div>

      <div class="flex items-center gap-3 sm:gap-4 justify-center" data-usal="fade-r delay-[400]">
        <div class="flex items-center h-full gap-3">
          <Icon name="mdi:refresh-circle" size={48} class="text-primary sm:w-16 sm:h-16" />
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold">~<span data-usal="count-[80]">80</span>%</div>
          <div class="w-28 sm:w-32 text-sm sm:text-base">Volvió a confiar en nosotros</div>
        </div>
      </div>
    </div>
  </section>
</div>
