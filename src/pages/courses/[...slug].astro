---
export const prerender = false;

const { slug } = Astro.params;
if (slug == null || slug === '') {
  return Astro.redirect('/cursos' + Astro.url.search, 308);
}
return Astro.redirect(`/cursos/${slug}` + Astro.url.search, 308);
---
